---
import "@fontsource-variable/atkinson-hyperlegible-next";
import "../../styles/global.css";
import Base from "../../layouts/Base.astro";
import MultiQuizView from "../../components/MultiQuizView.astro";
import { languages } from "../../i18n/ui";
import { getLangFromUrl, useTranslations } from "../../i18n/utils";
import MultiQuizViewFallback from "../../components/MultiQuizViewFallback.astro";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

export async function getStaticPaths() {
  const paths = Object.entries(languages).map(([lang, label]) => {
    return { params: { lang } };
  });

  return paths;
}
---

<Base>
  <h1>🧠 {t("title")}</h1>
  <MultiQuizView server:defer>
    <MultiQuizViewFallback slot="fallback" />
  </MultiQuizView>
</Base>
